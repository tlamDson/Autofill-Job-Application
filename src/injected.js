// injected.js — MAIN world script (page context)
// Trực tiếp đụng React/Vue state, setNativeValue, form filling.
// Phase 1.15 sẽ implement đầy đủ.

console.debug('[Autofill] injected script loaded (MAIN world)');
